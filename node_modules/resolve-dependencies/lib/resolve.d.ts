import { File, Files } from './file';
import { ResolveDepOptions } from './options';
import { resolveSync } from './node-loader';
export default function resolveFiles(...options: (Partial<ResolveDepOptions> | string)[]): {
    entries: Files;
    files: Files;
    warnings: string[];
};
export { File, Files, resolveSync };
